<% @user ||= current_user %>
<div class="stats">
	<a href="<%= following_user_path(@user) %>">
		<strong id="following" class="stat">
			<%= @user.followed_users.count %>
		</strong>
		following
	</a>
	<a href="<%= followers_user_path(@user) %>">
		<strong id="followers" class="stat">
			<%= @user.followers.count %>
		</strong>
		followers
	</a>
</div>
<br/>
<p>To follow other users click on the <%= link_to "USERS", users_path %> selection in the menu, find a user you'd like to "follow" and click the "follow" button next to their name.</p>